<UserControl x:Class="VideoCreatorWPF.Views.CharacterListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:VideoCreatorWPF.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="300" d:DesignHeight="400">
    <Border Background="#27272a" BorderBrush="#3f3f46" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#18181b" BorderBrush="#3f3f46" BorderThickness="0,0,0,1" Padding="10">
                <Grid>
                    <TextBlock Text="キャラクター" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
                    <Button Content="+ 追加" Command="{Binding AddCharacterCommand}"
                            HorizontalAlignment="Right" Height="25" Width="70"/>
                </Grid>
            </Border>

            <!-- Character List -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Characters}" Padding="5">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1e1e22" BorderBrush="#3f3f46" BorderThickness="1"
                                CornerRadius="4" Margin="5" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding SpeakerId, StringFormat='話者ID: {0}'}"
                                               Foreground="#a1a1aa" FontSize="10"/>
                                    <Rectangle Height="3" Fill="{Binding ColorBrush}" Margin="0,3,0,0"/>
                                </StackPanel>

                                <Button Grid.Column="1" Content="×" Width="25" Height="25"
                                        Command="{Binding DeleteCommand}"
                                        Background="Transparent" BorderThickness="0"
                                        Foreground="#a1a1aa" FontSize="14"
                                        Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
